RSpec.describe Activerecord::Increment::And::Return do
  it "has a version number" do
    expect(Activerecord::Increment::And::Return::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
